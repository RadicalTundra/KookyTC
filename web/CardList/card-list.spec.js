'use strict';

describe('myApp.CardList module', function() {

  beforeEach(module('myApp.cardList'));

  describe('CardList controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var cardListCtrl = $controller('cardListCtrl');
      expect(cardListCtrl).toBeDefined();
    }));

  });
});
