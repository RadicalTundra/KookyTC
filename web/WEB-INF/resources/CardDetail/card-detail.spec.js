'use strict';

describe('myApp.CardDetail module', function() {

    beforeEach(module('myApp.cardDetail'));

    describe('CardDetail controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var cardDetailCtrl = $controller('cardDetailCtrl');
            expect(cardDetailCtrl).toBeDefined();
        }));

    });
});
