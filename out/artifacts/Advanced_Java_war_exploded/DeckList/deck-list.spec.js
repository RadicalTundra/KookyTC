'use strict';

describe('myApp.DeckList module', function() {

    beforeEach(module('myApp.deckList'));

    describe('DeckList controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var deckListCtrl = $controller('deckListCtrl');
            expect(deckListCtrl).toBeDefined();
        }));

    });
});
